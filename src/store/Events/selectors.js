//get all the events with chef ([])
export const selectEvents = (reduxState) => reduxState.Events.Events;

//get one specific event with chef ([])
export const selectEvent = (reduxState) => reduxState.Events.Event;
